{{- if .Values.service.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "hpc-milk.serviceName" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "hpc-milk.labels" . | nindent 4 }}
  {{- with .Values.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  selector:
    {{- include "hpc-milk.selectorLabels" . | nindent 4 }}
  ports:
    {{- toYaml .Values.service.ports | nindent 2 }}
  type: {{ .Values.service.type }}
{{- end }}
